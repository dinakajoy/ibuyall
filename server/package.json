{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "description": "An ecommerce application",
  "main": "app",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "dotenv": "^9.0.2",
    "express": "^4.17.1",
    "swagger-ui-express": "^4.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.3",
    "jsdoc": "^3.6.6",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-start": "nodemon --exec ts-node --files src/server.ts",
    "build": "rm -rf build && tsc -p .",
    "start": "node ./src/server"
  },
  "author": "Odinaka Joy"
}
